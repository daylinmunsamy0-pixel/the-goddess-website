# The Goddess Website

Production-ready static website for The Goddess.

## Deployment

The site files are at the bundle root. For the GitHub/Netlify workflow, upload this package as `site-bundle.zip` at the repository root; `netlify.toml` extracts the bundle during the Netlify build.

## Local QA

Run `node tests/site.test.js` from the project root.
